# Telegram 新闻监控项目评估与修订报告

> **历史文档（只读基线）**  
> 本报告对应审计基线提交 `76f7aeb95e00d9acdb84d862ee44c6fe33ebcd3e`（2026-09-10），描述的是当时的缺口与本地修订，**不是**当前仓库的行为说明。  
> 后续已在 `main` 上继续演进（CodeBuddy CLI、对等 Group、`old_photos` / `wechat_photo`、主题策略等）。线上行为、配置项与部署步骤以 [README](README.md) 为准。  
> 文中「已实现」仅指随该报告交付的本地副本，不要据此判断今日仍待修复或已经修复的条目。

评估日期：2026-09-10（北京时间）。对象：[bazz1111/tg_news_monitor](https://github.com/bazz1111/tg_news_monitor)，审计基线提交 `76f7aeb95e00d9acdb84d862ee44c6fe33ebcd3e`。

本报告基于源码、测试和本地模拟。已修改本地副本并验证；未连接你的部署、数据库、付费模型或飞书机器人，未向 GitHub 推送。以下“已实现”均指随报告交付的本地版本。

## 1. 结论与建议参数

项目适合个人或小团队的公开频道快讯筛选，但目前不能把“抓到一条帖子”直接等同于“发现一条新事件”。你的三个问题来自三个独立缺口：采集与模型共用执行流程、去重仅识别消息编号、没有可靠的新闻年龄限制。单纯把轮询改为五分钟，无法一起解决这些问题。

建议先使用以下均衡配置，观察三天后根据真实流量调整，不存在脱离频道发文量、模型响应时间与预算的“最优固定阈值”。配置片段见 `monitor-tuning.env.example`。

| 参数 | 建议初值 | 作用 |
|---|---:|---|
| 采集周期 | 60 秒，抖动 ±约10秒 | 尽早获取新帖，本身不调用模型 |
| 频道间隔 | 2 秒 | 避免集中请求公开预览页面 |
| 模型最小调用间隔 | 180 秒 | 高频新闻不会每轮都触发模型 |
| 正常批次触发数量 | 12 条有效、去重后的候选 | 原来的3条太容易被转载填满 |
| 最长正常等待 | 600 秒 | 低流量时不无限等待凑数 |
| 单批上限 | 20 条，每条截取800字符 | 限制输入；溢出留到下一轮 |
| 每日调用上限 | 288 次，按 UTC 日重置 | 防止异常流量持续消费；不是每日消费目标 |
| 单次输出上限 | 3000 tokens | 限制摘要和分析篇幅 |
| 新闻年龄上限 | 1800 秒 | 同时约束频道发布时间与模型提取的事件时间 |
| 推送评分 | 至少7分 | 恢复配置项在实际推送路径中的作用 |
| 同批卡片间隔 | 2 秒 | 降低人为排队延迟 |

普通消息在“满12条”或“等待10分钟”时进入评估；少量明确紧急关键词，如强震、导弹袭击、紧急降息、交易暂停，可以提前触发评估，但仍受冷却、预算、评分和时效限制。关键词只是调度提示，会漏判，也可能误触发。

重要消息约3–5分钟、普通消息约10分钟是正常负载下的目标，而非保证：还要加上采集轮次、网络和模型耗时；未被本地识别为紧急的孤立新闻仍可能等待10分钟。预算用尽时停止模型调用，继续采集，超过时限的候选会被丢弃，不在第二天补发旧快讯。

**预算与时效存在真实冲突。** 持续每3分钟调用一次最多480次/天，288次预算在这种极端负载下约14.4小时就会耗尽。如果要求全天持续3分钟评估，应把每日上限提高至至少480并接受相应成本，或采用5分钟最小间隔。当前没有自动按小时平滑分配预算。

## 2. 根因与修订结果

| 问题 | 原实现证据与影响 | 本地修订 |
|---|---|---|
| 模型调用过于频繁 | `core/runner.py` 只判断数量/最长等待，没有最小间隔；同一热点多频道转载即可触发 | SQLite持久化调用预约、最小间隔、每日次数；失败也占用一次预约 |
| 单次输入膨胀 | 全部 pending 一次交给模型，只有单帖字符限制，没有批次上限 | 最多20条，紧急候选优先、同优先级较新消息优先，其余保留 |
| 模型阻塞采集 | 连续执行采集→模型重试→卡片间隔→睡眠 | 守护进程使用一个后台评估线程；主线程继续采集；最多一个评估任务在运行 |
| 同一新闻重复推送 | `storage/repository.py` 的唯一性只有频道与消息ID，无法识别跨频道转载 | 规范化正文指纹、24小时投递记录、同批去重；模型获得近期已推摘要，比较是否有新事实 |
| 模型重复/编造来源 | 返回条目未严格核对本批输入，重复ID可发送多次 | 只接收本批真实来源，每个来源仅一次 |
| 新抓到旧消息 | 不检查 `published_at`；首次部署尤其明显 | 模型前及每条推送前检查年龄；超过30分钟或明显未来时间拦截 |
| 时间缺失变成最新 | 解析器和数据库读取失败会使用当前时间 | 改为明确过期时间，避免伪装成新消息；不带时区也不能通过时效门槛 |
| 今天转发昨天的事 | 频道时间不是事件时间；提示词缺少当前时间 | 给模型当前UTC，要求提取 `event_at`；未知、过旧的事件不进即时推送 |
| 模型故障导致丢消息 | API/解析失败返回“空新闻”，随后所有候选被永久标记过滤 | 失败抛出异常，保留候选，冷却后可再次尝试，过期后清理 |
| 投递重试制造重复风险 | Webhook响应丢失时可能已送达，立即重试可能重复 | 默认一次尝试，发送前持久化认领；结果不明记 `unknown`，不自动重试 |
| 时间显示及来源不清 | 卡片只有笼统“时间”；主流程没有原文入口 | 区分频道发布时间与事件时间，附原文链接；修复Windows缺少时区数据导致的报错 |
| 过滤误伤 | 加密正则包含单字“聪” | 改为“中本聪”，保留加密内容过滤 |

关键源码依据：[原runner](https://github.com/bazz1111/tg_news_monitor/blob/76f7aeb95e00d9acdb84d862ee44c6fe33ebcd3e/src/tg_news_monitor/core/runner.py)、[原模型客户端](https://github.com/bazz1111/tg_news_monitor/blob/76f7aeb95e00d9acdb84d862ee44c6fe33ebcd3e/src/tg_news_monitor/evaluator/grok_client.py)、[原解析器](https://github.com/bazz1111/tg_news_monitor/blob/76f7aeb95e00d9acdb84d862ee44c6fe33ebcd3e/src/tg_news_monitor/scraper/parser.py)、[原存储](https://github.com/bazz1111/tg_news_monitor/blob/76f7aeb95e00d9acdb84d862ee44c6fe33ebcd3e/src/tg_news_monitor/storage/repository.py)。

## 3. 架构师视角：保留什么，删什么

保留 SQLite、现有 HTTP 客户端与解析器。对于十几个公开频道，引入 Kafka、Redis、向量数据库或多级模型链没有必要。先把执行边界和状态记录做对。

```mermaid
flowchart LR
    A[定时采集公开频道] --> B[(SQLite候选池)]
    B --> C[过期过滤 / 加密过滤 / 正文去重]
    C --> D[数量或时间或紧急条件]
    D --> E[持久化冷却与每日预算]
    E --> F[单后台线程：限量批次评估]
    G[(近期投递摘要)] --> F
    F --> H[来源校验 / 事件时效 / 评分]
    H --> I[投递认领 → 飞书单次尝试]
    I --> G
```

已减少的冗余：主流程对未选中消息重复写入评价；提示词一边强调精选，一边要求优先凑3–5条；同批总览在单卡模式下仍要求长篇生成。新版提示词缩短，总览可为空，摘要与要点尽量简短，不相关的市场影响不强行展开。

建议后续清理、此次未冒险删除的部分：旧的逐帖评分/异步评估/多级回退代码，Grok命名与DeepSeek实际实现混用，多套配置回退解析和兼容别名。这些可增加理解成本，但删除前应检查外部调用者。原项目的一些端到端测试实际上手写了另一套流程，不能代替真实 runner 的测试；本次补充了实际 runner 回归测试。

仍需改进的可靠性：当前不是完整持久化发件箱。模型评价已落库但尚未发送时如果进程崩溃，可能漏推；投递认领后崩溃也会被视为不明状态。此版本优先避免重复发送，没有承诺“既不重复又绝不丢失”。如果你要求必达，下一步应保存待发卡片、增加待核实投递列表，并采用支持业务幂等键的投递通道。不能把简单Webhook重试包装成严格 exactly-once。

公开预览页也不是完整消息总线：这里未增加翻页补采、消息编辑追踪、附件/OCR或页面变化告警。高频频道在两轮间超过页面窗口时可能漏采；PDF研报只有附件没有正文时，系统无法可靠解读。采集与模型已解耦，但频道仍依次抓取，一个频道网络超时仍会拖长这一轮。

## 4. 产品经理视角：即时快讯不应承担所有信息任务

你关注的军事、全球突发和宏观数据适合即时推送；投行研报、投资人长文与技术论文更适合单独的“新发布研究”摘要。研究结论可能依据过去一季的数据，这不意味着它是过期信息，但也不能伪装成刚刚发生的突发事件。

本次交付优先修复即时快讯链路，采用严格30分钟门槛。**新发布但无法识别近期事件时间的研报和长文也可能被拦截。** 长文/研报的独立摘要通道未实现，不应把这版描述为已完整覆盖研报阅读需求。后续若加该通道，应明确展示报告发布日期、数据截止日、作者/机构及原文，允许更长年龄，单独计预算，避免混入紧急推送。

不要把频道数量当覆盖率。评估频道应看它带来的独家有效新闻、来源可验证性、转述偏差和重复占比。此次未采样你的真实频道流量，因此没有声称某个频道“必删”，也没有给出未经测量的去重率。加密货币专门来源不应加入组合；目前保留代码内加密过滤，但关键词规则仍可能误伤混合主题或遗漏变体，不能保证零漏网。

## 5. 用户视角：一条消息应回答什么

卡片至少要能区分：发生了什么、事件何时发生、频道何时发布、哪里能看原文、哪些句子是事实、哪些是模型分析。本次增加来源与事件时间，但事件时间依然由模型依据原文提取，不等于权威核验。

同一事件的再次推送，应仅在出现实质变化时发生，例如“初报事故”到“官方确认伤亡”或“提议加税”到“正式签署”。只更换标题、翻译语言或转述口吻，不构成新消息。模型提示已使用这一原则；本地指纹保留数字与否定词，避免把25基点和50基点、批准和未批准强行合并。

严格旧闻策略的代价是漏报：无法确认事件时间的内容直接不推。模型也可能错误提取时间，因此这套方案能消除已知工程缺口，不能保证任何错误报道或旧闻都不会通过。对真实性要求更高的场景，需要回查权威原始来源。

## 6. 如何平衡成本与时效

先看“有效候选产生速度”，再定模型周期。采集一百条不等于需要一百次API调用：旧闻、广告、加密内容和完全重复正文在本地过滤，只有去重后的候选参与数量门槛。

设有效候选速率为每分钟 λ 条，数量门槛12，最长等待10分钟，模型冷却3分钟。在稳定负载、预算充足且处理够快时，正常批次间隔可粗略理解为 `max(3分钟, min(12/λ分钟, 10分钟))`。紧急提示可绕过数量和等待条件。该式不包含网络时间、积压、采集抖动与模型失败，不能用作SLA。

用真实API返回的 `usage.total_tokens` 统计，不把字符数当token数。数据库新增 `digest_calls` 保存预约时间和已获得的用量；缺失用量是未知，不是零消费。调用异常也可能产生费用，以服务商账单为准。[DeepSeek官方接口说明](https://api-docs.deepseek.com/api/create-chat-completion/)定义了 `usage` 和 `max_tokens`，后者只限制输出，不能限制整日总token。

例如平均一次实际使用6000输入token、1500输出token，则144次约108万token/天，288次约216万token/天。这只是演算，不是项目实测。费用还须分别乘输入/输出单价及缓存命中价格。本次没有改动模型名称，也未假设当前价格。

本地版本实现的是“调用次数硬上限＋输入规模限制＋输出上限”，**不是严格的每日token硬上限**。输入正文最多20×800字符；历史最多20条×300字符，避免上下文无限增长。达到输入截断处可能丢失长文末尾的关键时间或否定信息，原文链接用于追溯。

建议三种起点：

| 目标 | 采集 | 模型最小间隔 | 正常最长等待 | 日调用上限参考 |
|---|---:|---:|---:|---:|
| 时效优先 | 60秒 | 120秒 | 300秒 | 720 |
| 均衡，建议先试 | 60秒 | 180秒 | 600秒 | 288 |
| 成本优先 | 120秒 | 600秒 | 900秒 | 144 |

不要一次改所有参数。先保持60秒采集，观察去重后候选量、每次token和推送价值；若大量批次不到3条且价值不高，延长等待；若每天很早触顶，增加预算或冷却时间；若过期丢弃明显增多，应检查采集异常、模型耗时与积压，而不是延长新闻寿命掩盖问题。

## 7. 去重能力的准确边界

1. 同频道同消息ID：原有数据库唯一约束保留。
2. 跨频道完全相同正文：Unicode/大小写/空白规范化后生成SHA256指纹，同批及24小时投递记录拦截。数字和否定保留；附加推广尾巴、换句式不会被硬合并。
3. 同批改写或跨语言转载：交给一次批量模型评估合并。
4. 跨批改写：把24小时内最近20条投递摘要作为历史。只有新事实才选择；20条以外的历史不保证语义去重。
5. 模型返回重复来源：本地拦截。
6. 发送结果不明：不自动重试，记录 `unknown`。这包括实际已送达但响应丢失的情况。

这是一套有明确上限、成本可控的方案。没有增加额外的逐条“相似度模型”API调用，也没有宣称语义去重能百分之百可靠。若实测仍有明显跨日或大量改写重复，再考虑事件实体/日期索引与局部检索，而不是一开始就引入向量数据库。

## 8. 验证记录

原始基线：129通过、7失败。4项runner失败与Windows时区数据缺失有关，3项与测试仍检查旧版卡片结构有关。

修订后全量：**140通过**。运行命令：

```text
python -m pytest -q --basetemp=tests/.handoff_verify_tmp
git diff --check
```

新增测试覆盖持久化冷却/次数限制、跨实例指纹认领、过期/未来/无时区时间、模型失败保留pending、批量溢出保留、跨频道相同正文、模型重复及编造来源、过期事件不推送、输出token上限，以及模型工作线程等待时采集仍能继续。

同步修正旧测试的固定“新闻日期”和飞书卡片2.0按钮结构预期。测试中的网络和模型均为模拟，没有验证真实页面长期可用性、真实模型语义判断、真实token节省比例或飞书线上投递。旧端到端测试不是生产runner全链路，不能仅凭其名称推断线上保障。

## 9. 应用、观察与回滚

1. 在部署环境先停旧实例，备份数据库及配置；如果备份SQLite文件，需包含WAL一致性，优先使用SQLite备份工具。不要在新旧实例同时运行时切换。
2. 应用本地代码或随附补丁。补丁基于上述提交，其他版本先检查差异。
3. 将 `monitor-tuning.env.example` 的参数合并入现有配置，保留自己的频道、API密钥、Webhook和数据库路径。注意OS/容器环境变量优先于 `.env`，`.env` 优先于YAML；旧配置不更新就不会自动采用12条/10分钟。
4. 使用持久化数据库。新增两张表由程序自动创建；不要为了“重置”而清空数据库，否则历史去重与预算记录也消失。当前建议单实例运行。新投递历史从升级后开始记录，未迁移旧版已经推送的语义摘要，首次升级存在历史盲区；原有消息ID去重仍保留。
5. 重新构建镜像或重启实际Python服务，再观察。`--once` 可能因门槛未达到而不调用模型，这是正常行为。重启不会绕过已持久化的预算冷却。
6. 回滚代码时保留数据库备份；新增表不影响原表，但旧代码不会执行新时效与成本保护。

建议上线后连续三天记录：每频道抓取成功率、去重后候选量、推送年龄分布、每日调用/用量、过期原因计数、未知投递数量，以及人工抽样的重复率与有效率。这些指标尚未做成管理页面，不能把当前日志当完整监控平台。

可用以下SQL检查关键状态（只读）：

```sql
SELECT date(started, 'unixepoch') AS utc_day,
       count(*) AS calls, sum(tokens) AS known_tokens,
       sum(tokens IS NULL) AS unknown_usage_calls
FROM digest_calls GROUP BY utc_day ORDER BY utc_day DESC;

SELECT filter_reason, count(*) FROM posts
WHERE is_filtered=1 GROUP BY filter_reason;

SELECT datetime(claimed,'unixepoch') AS utc_time, status, summary
FROM delivery_claims WHERE status='unknown' ORDER BY claimed DESC;
```

后两条是现有留存范围内的统计，不应当作严格的每日指标。`delivery_claims` 仅保留约24小时的去重窗口；需要长期审计时应另行归档。

最终建议：先用本次修订解决即时链路的明确缺口，保持保守时效限制，再根据真实三天数据决定预算与周期。不要以放宽旧闻门槛、增加重复来源或频繁重试来换取表面上的“消息不断”。
